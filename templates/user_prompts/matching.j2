{% extends "user_prompts/base_exercise.j2" %}
{% block task_instruction %}
Generate a **Matching Exercise**.
1. Create pairs of items (e.g., Word-Definition, Word-Image, Question-Answer).
2. If using images, generate `asset_specs` in the `generation` field for `image_url`.
3. Ensure 4-5 pairs.
{% endblock %}
